{
	"name": "@lab/cosignal-oracle",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"description": "Naive, obviously-correct reference model + randomized referee harness for cosignal v1 (spec/cosignal-v1.md).",
	"scripts": {
		"test": "vitest run",
		"typecheck": "tsc -p tsconfig.json"
	},
	"devDependencies": {
		"@types/node": "24.13.2",
		"typescript": "6.0.3",
		"vitest": "4.1.6"
	}
}
