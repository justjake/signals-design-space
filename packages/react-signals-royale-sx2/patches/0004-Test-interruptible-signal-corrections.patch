From d9034d1ca33e970cd12feb1184d9230df4b7c717 Mon Sep 17 00:00:00 2001
From: Jake Teton-Landis <just.1.jake@gmail.com>
Date: Wed, 8 Jul 2026 13:49:05 -0400
Subject: [PATCH 4/4] Test interruptible signal corrections

---
 .../__tests__/ReactExternalSignals-test.js    | 52 ++++++++++++++++++-
 1 file changed, 51 insertions(+), 1 deletion(-)

diff --git a/packages/react-reconciler/src/__tests__/ReactExternalSignals-test.js b/packages/react-reconciler/src/__tests__/ReactExternalSignals-test.js
index 7ee58de4ef..0fd3b61ec4 100644
--- a/packages/react-reconciler/src/__tests__/ReactExternalSignals-test.js
+++ b/packages/react-reconciler/src/__tests__/ReactExternalSignals-test.js
@@ -12,7 +12,11 @@
 
 let React;
 let ReactNoop;
+let Scheduler;
 let act;
+let waitFor;
+let waitForAll;
+let assertLog;
 let protocol;
 
 describe('external signals protocol', () => {
@@ -20,7 +24,12 @@ describe('external signals protocol', () => {
     jest.resetModules();
     React = require('react');
     ReactNoop = require('react-noop-renderer');
-    act = require('internal-test-utils').act;
+    Scheduler = require('scheduler');
+    const InternalTestUtils = require('internal-test-utils');
+    act = InternalTestUtils.act;
+    waitFor = InternalTestUtils.waitFor;
+    waitForAll = InternalTestUtils.waitForAll;
+    assertLog = InternalTestUtils.assertLog;
     protocol =
       React.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE.L;
   });
@@ -79,6 +88,47 @@ describe('external signals protocol', () => {
     stop();
   });
 
+  it('lets urgent work interrupt a lane-pinned correction', async () => {
+    let updateSlow;
+    let updateUrgent;
+    function Text({text}) {
+      Scheduler.log(text);
+      return text;
+    }
+    function App() {
+      const [slow, setSlow] = React.useState(0);
+      const [urgent, setUrgent] = React.useState(0);
+      updateSlow = setSlow;
+      updateUrgent = setUrgent;
+      React.useLayoutEffect(() => Scheduler.log('Commit'));
+      return (
+        <>
+          <Text text={'Slow: ' + slow} />
+          {slow === 1 ? (
+            <>
+              <Text text="A" />
+              <Text text="B" />
+              <Text text="C" />
+            </>
+          ) : null}
+          <Text text={'Urgent: ' + urgent} />
+        </>
+      );
+    }
+    const root = ReactNoop.createRoot();
+    await act(() => root.render(<App />));
+    assertLog(['Slow: 0', 'Urgent: 0', 'Commit']);
+    let lane;
+    React.startTransition(() => {
+      lane = protocol.getWriteLane();
+    });
+    protocol.runInLane(lane, () => updateSlow(1));
+    await waitFor(['Slow: 1', 'A']);
+    ReactNoop.flushSync(() => updateUrgent(1));
+    assertLog(['Slow: 0', 'Urgent: 1', 'Commit']);
+    await waitForAll(['Slow: 1', 'A', 'B', 'C', 'Urgent: 1', 'Commit']);
+  });
+
   it('brackets the host mutation phase', async () => {
     const phases = [];
     const stop = protocol.subscribe(event => {
-- 
2.50.1 (Apple Git-155)

