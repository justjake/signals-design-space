From dda89c18f591f13f764d3327d4fbc8adad132a8e Mon Sep 17 00:00:00 2001
From: Jake Teton-Landis <just.1.jake@gmail.com>
Date: Wed, 8 Jul 2026 13:46:34 -0400
Subject: [PATCH 3/4] Test lane-pinned signal corrections

---
 .../__tests__/ReactExternalSignals-test.js    | 21 +++++++++++++++++++
 1 file changed, 21 insertions(+)

diff --git a/packages/react-reconciler/src/__tests__/ReactExternalSignals-test.js b/packages/react-reconciler/src/__tests__/ReactExternalSignals-test.js
index 19b454ba91..7ee58de4ef 100644
--- a/packages/react-reconciler/src/__tests__/ReactExternalSignals-test.js
+++ b/packages/react-reconciler/src/__tests__/ReactExternalSignals-test.js
@@ -58,6 +58,27 @@ describe('external signals protocol', () => {
     stop();
   });
 
+  it('commits a correction in the lane supplied by the caller', async () => {
+    const events = [];
+    const stop = protocol.subscribe(event => events.push(event));
+    const root = ReactNoop.createRoot();
+    let update;
+    function App() {
+      const [value, setValue] = React.useState(0);
+      update = setValue;
+      return value;
+    }
+    await act(() => root.render(<App />));
+    let lane;
+    React.startTransition(() => {
+      lane = protocol.getWriteLane();
+    });
+    await act(() => protocol.runInLane(lane, () => update(1)));
+    const commit = events.filter(event => event.type === 'commit').pop();
+    expect(commit.lanes & lane).toBe(lane);
+    stop();
+  });
+
   it('brackets the host mutation phase', async () => {
     const phases = [];
     const stop = protocol.subscribe(event => {
-- 
2.50.1 (Apple Git-155)

