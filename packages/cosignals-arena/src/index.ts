/**
 * cosignals-arena combines the core signals API with the React bindings.
 *
 * Importing this entry registers the React integration. Code that does
 * not use React should import from `cosignals-arena/core` instead.
 */
export * from "./core.ts"
export * from "./react/index.ts"
