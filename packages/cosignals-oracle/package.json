{
	"name": "@lab/cosignals-oracle",
	"version": "0.0.0",
	"private": true,
	"description": "Naive, obviously-correct executable reference model and randomized referee harness for the cosignals package's concurrent semantics.",
	"type": "module",
	"scripts": {
		"test": "vitest run",
		"typecheck": "tsc -p tsconfig.json"
	},
	"devDependencies": {
		"@types/node": "24.13.2",
		"typescript": "7.0.2",
		"vitest": "4.1.6"
	}
}
