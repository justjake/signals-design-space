# react-signals-royale-sx1

React bindings for `signals-royale-sx1`. Writes join React's current lane, renders fold exactly the lanes in their pass, and each root records the batches its DOM has committed. The patched React build also exposes the exact host-mutation window for MutationObserver clients.
