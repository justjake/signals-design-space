{
	"name": "cosignals-react",
	"private": true,
	"version": "0.1.0",
	"license": "MIT",
	"description": "React bindings for cosignals: the useSignal, useComputed, useReducerAtom, and useSignalEffect hooks plus startSignalTransition, backed by an external-runtime bridge that keeps concurrent renders, transitions, and committed UI state consistent (tear-free).",
	"type": "module",
	"main": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"check": "tsc --noEmit -p tsconfig.json",
		"typecheck": "tsc --noEmit -p tsconfig.json",
		"test": "vitest run"
	},
	"dependencies": {
		"cosignals": "workspace:*",
		"react": "*",
		"react-dom": "*"
	},
	"devDependencies": {
		"@types/react": "^19.1.8",
		"@types/react-dom": "^19.1.6",
		"jsdom": "^26.1.0",
		"typescript": "6.0.3",
		"vitest": "4.1.6"
	}
}
