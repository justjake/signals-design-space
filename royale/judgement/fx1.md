# Judgement scorecard: fx1

**Verdict: issues** · fork LOC 80 · lib LOC 2307

## Gates (claimed vs observed)

All re-run by judge at final HEAD 48ada616 (fork tip 8af23b4d57), machine quiet ≥4 min. Typecheck: both packages tsc --noEmit exit 0. Engine suite: "Tests 195 passed (195)" incl. conformance "Tests 179 passed (179)" (reactive-framework-test-suite@0.0.2, untracked implemented, no skips). Oracle: default in suite + deep sweep ORACLE_SEEDS=1200 "Tests 1 passed (1)" (~1.8s); oracle is a genuine 488-line fuzzer (mulberry32 seeds, ~90-step schedules, shrinker, 2 pinned regressions). Leak audits: engine gc-leaks 4/4, react gc-leaks 2/2; both vitest configs verified pool:'forks' + execArgv ['--expose-gc']. Real-React gate: react package "Tests 26 passed (26)" (scenarios 1-18 across scenarios/scenarios2/hooks/gc-leaks). Shared battery: "Tests 25 passed (25)"; tamper check: battery.spec.tsx, royale-types.ts, vitest.config.ts, global.d.ts byte-identical to canonical; ADAPTER.ts + package.json wiring-only. Fork protocol: ReactDOMSignalScheduler "Tests: 8 passed, 8 total"; upstream adjacent 10-suite set "Tests: 1 skipped, 121 passed, 122 total" (matches claim). Pristine fork: git am of the 2 patches onto a fresh worktree at base e71a6393e6 → 0-line diff vs royale/fx1-react; fork rebuilt from that tree via fork/build-react.sh ("Built: 19.3.0 (8af23b4d57)") and battery re-passed 25/25 on the fresh build; built artifacts carry the __SIGNALS_ROYALE_FX1__ bridge. Fork numstat: 80 (44 new ReactFiberSignalScheduler.js, +23 WorkLoop, +13 RootScheduler), no __tests__ leakage into the metric. react-bench spot check: transition p95 urgent→commit 5.63 ms (fx1) vs 97.3 ms (useSyncExternalStore baseline) — headline claim verified. REPORT milomg table (geomean 1.58x alien, creation suites 4-7x) taken as reported with disclosed noise caveat; run shape deviates from ROUND2's dist/isolated.js (used per-process dist/index.js), disclosed in REPORT.

## Shared battery

25/25 pass (judge re-run, twice: once pre-rebuild, once against a fork freshly rebuilt from the patch series). Kit untampered: all four canonical files byte-identical to /Users/jitl/src/alien-signals-opt/royale/verify/battery; ADAPTER.ts is a one-line re-export; package.json links only.

## Red flags

No gaming: no tamper, no incumbent imports (grep clean for cosignals/concurrent-solid-react/dalien), adapters pure wiring, no line-golfing (prettier-normalized, heavily commented, readable), LOC self-count matches canonical counter exactly (80/2307). Two feature-honesty failures found by probe, both claimed "done" in REPORT §5 (evidently unwitting — neither entry tests nor the shared battery cover them): (1) CONFIRMED — latest() context rule violated in render bodies: inside an urgent render body during a live held transition, latest(a) returns the transition draft (3) while the render's own world shows canonical (1) = the tear RULES forbids ("inside a render pass it resolves that context's own world"). Mechanism: latest() is context-bound only via activeFrame/activeEvalRun, which are set during engine evaluations, never during a React render body, so a direct call falls through to latestFrame() (all open episodes). Inside computed evaluations latest() IS correct (probe passes: canonical eval sees canonical, draft-world eval sees its own draft). (2) CONFIRMED — refresh() inside a transition is a complete no-op in the rule's core case (unchanged inputs): no refetch starts during the transition, at retirement, or on any later read; the refresh is silently swallowed (probed: fetch generation never advances, value never updates, isPending stays false after commit). Mechanism: the transition render takes frameDerivedRead's canonical-share fast path (!frame.touches(d) && newestSourceSeq <= pinSeq), which never consults the episode's refreshMarks; engine-level tests pass because pin=-1 frames bypass that path, and the canonical (non-transition) refresh path is genuinely correct (eager refetch, stale serves with no fallback flash, latest-wins verified incl. zombie-settlement ordering). Minor: REPORT's pre-Round-2 LOC (2297) and mid-run claims were measured before final tuning, corrected to 2307 in the final commit; ROUND2 milomg runner deviation disclosed. Judge probes preserved at /tmp/judge-probes/tests/.

## Notes

Entry-specific findings. (a) Inversion shipped as "store owns batch identity and lane assignment", not full scheduling: the runtime claims React transition lanes itself (the 80-line fork exposes requestTransitionLane(null) through a generic SignalSchedulerBridge global) and pins one lane per engine episode on the transition token; every delivery for that episode — original bumps, corrective joins for mid-transition mounts (episodeAffects at subscribe), settlements — dispatches under that pinned lane, and the ENGINE owns episode lifecycle (auto-retires never-delivered episodes on its own microtask; retires when every delivered root commits; folds ops with React updater-queue replay arithmetic). React's role reduces to honoring the pin plus three fact reports (pass start, per-root commit, exact mutation window). So it did NOT collapse to a passive lane-facts listener — lane grouping and batch retirement are genuinely store-driven — but React still owns render timing; "tells React what to render when" became "tells React which lane every update belongs to, forever". Same keystone observable as the incumbent runInBatch+pin, novel mechanism (write-time classification via ReactSharedInternals.T token identity, no React-side batch API), and the 80-line fork vs incumbent 1510 is the field's standout ratio. (b) "Eager refresh" verified: refresh means fetch-now on the canonical path with stale serving and latest-wins races (probes pass); it does NOT mean drop-stale-and-suspend; but the transition-owned refresh path is broken outright (red flags). (c) Not over-engineered relative to capability: 2307 counted lines is <half of alt-a/alt-b, and the mass is real mechanism (MVCC pinned bases + per-cell update queues with scheduling-order replay, world-keyed async contexts adopted into canonical at retirement so commits don't refetch, cold/hot edge discipline where cold nodes hold forward pointers only for leak-freedom, value-judged CHECK marks so write-then-revert batches net to nothing, ring tracer). The cost shows elsewhere: milomg 1.58x alien geomean (creation-heavy suites 4-7x — honestly reported), and the refresh bug lives exactly at an optimization fast-path × rare-feature intersection, a complexity-risk data point. Readability is high; house style followed. Ideas worth stealing: store-claimed pinned lanes (the whole fork in 44+36 lines), episode auto-retire for never-observed transitions, value-stamped edges for revert coalescing, adopt-world-contexts-at-retirement. Report honesty otherwise excellent: known-gaps section is specific and accurate (isPending topology over-reporting, lane-pool entanglement), Round 2 transcript matches my reproductions line for line. Verdict "issues" not DQ: every declared gate is genuinely green and reproducible; the two violations are in required-feature fine print no shared test covers, claimed done in good faith without tests — rank-relevant, and 3/9 judged entries reportedly share failure (1).
