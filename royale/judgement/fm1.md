# Judgement scorecard: fm1

**Verdict: clean (after two fix rounds; originally issues)** · final fork LOC 188 · final lib LOC 1780 · fork LOC 188 · lib LOC 1777

## Gates (claimed vs observed)

| Gate                                                                                           | Claimed                                       | Observed (final tree, re-run by judge)                                                                                                                                                                                                   |
| ---------------------------------------------------------------------------------------------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Typecheck engine (`npx tsc --noEmit -p tsconfig.json`)                                         | PASS                                          | PASS (ran twice, incl. after final commits)                                                                                                                                                                                              |
| Typecheck react                                                                                | PASS                                          | PASS                                                                                                                                                                                                                                     |
| Conformance `npx vitest run tests/conformance.spec.ts`                                         | 179/179, no skips                             | 179 passed (179), 0 skipped                                                                                                                                                                                                              |
| Oracle default `npx vitest run tests/oracle.spec.ts`                                           | PASS (300 seeds x 90 steps)                   | PASS — verified spec: real model-based fuzzer (mulberry32 PRNG, naive replay model, canonical/latest/snapshot checks per step, greedy shrink), SEEDS env-tunable default 300                                                             |
| Oracle deep `FUZZ_SEEDS=1200`                                                                  | PASS                                          | PASS (1 test, 592ms of fuzz)                                                                                                                                                                                                             |
| Leak audit `npx vitest run tests/gc-leaks.spec.ts`                                             | PASS (4 tests)                                | 4 passed (4); genuine: FinalizationRegistry + global.gc, pool:'forks' + execArgv --expose-gc confirmed in vitest.config.ts; covers dropped atoms/computeds, disposed effects, quiescence pin/past-value scrub, retired-batch reclamation |
| Engine suite `npx vitest run`                                                                  | 211 (211)                                     | 211 passed (211), 4 files                                                                                                                                                                                                                |
| Real-React gate `npx vitest run` (react pkg)                                                   | 25 (25) [final REPORT; earlier draft said 24] | 25 passed (25), 4 files — all 18 RULES scenarios present and verified verbose; jsdom, raw createRoot+act, no RTL                                                                                                                         |
| Fork protocol `yarn test --no-watchman ReactSignalSeam`                                        | 6 passed, 6 total                             | 6 passed, 6 total (re-run at final fork HEAD e21af85707)                                                                                                                                                                                 |
| Upstream adjacent (10 suites)                                                                  | 13 suites; 121 passed, 1 skipped              | 13 suites passed; 121 passed, 1 skipped, 122 total (re-run at final fork HEAD)                                                                                                                                                           |
| Fork build `build.sh`                                                                          | (implied)                                     | PASS — "Built: 19.3.0 (e21af85707)"; patches/ (0001-0003) verified byte-equivalent to royale/fm1-react branch commits vs base e71a6393e6                                                                                                 |
| Shared battery                                                                                 | 25 (25)                                       | 25 passed (25) against the freshly rebuilt final fork                                                                                                                                                                                    |
| Fork LOC                                                                                       | 188                                           | 188 (exact: ReactSignalSeam.js 113 + WorkLoop 69 + react-dom/client 6; recounted at final HEAD, both via raw numstat and count-loc.mjs)                                                                                                  |
| Library LOC                                                                                    | 1725                                          | **1777** — claim is stale (see redFlags)                                                                                                                                                                                                 |
| Benchmarks (milomg ~1.7x alien geomean; react-bench fanout +2%/transition p95 2.1ms/mount +9%) | reported                                      | NOT re-run (shared machine, not a pass/fail gate); REPORT is candid that fm1 is ~1.7x SLOWER than alien geomean (below incumbent parity) and honest about the uSES-baseline transition-row caveat — honest reporting of a weak benchmark |

## Shared battery

25/25 passed (observed twice: once against build 8c2e8cb6f5, once against final build e21af85707). Tamper check: battery.spec.tsx, royale-types.ts, vitest.config.ts, global.d.ts, tsconfig.json, README.md all byte-identical to canonical /Users/jitl/src/alien-signals-opt/royale/verify/battery/. ADAPTER.ts is a one-line re-export of the entry's royale/adapter.ts (wiring only). package.json differs only by swapping the incumbent link for react-signals-royale-fm1 (wiring only). No stubs, mocks, or global monkey-patching found; the runtime registers through the fork's exported seam entry points and reads React's documented-unstable transition internals (\_\_CLIENT_INTERNALS.T) — a userland read, not a patch. Verdict: NOT tampered.

## Red flags

1. MOVING TARGET DURING JUDGEMENT (process flag, orchestrator should tighten deadlines): the contestant was still committing while judging ran. Judging started ~14:45; commits landed at 14:47 (engine perf pass, +110 lines core.ts), 14:48 (adapter-load spec, gitlink pin), 14:50 (fork commit e21af85707 'Flow-clean casts', patch 0003), 14:51:43 ('Round 2 final'), 14:52:22 (final amend). My first pass over gates therefore hit a pre-final tree. Mitigation: I re-ran EVERY gate (typechecks, conformance, oracle default+1200, leaks, engine 211, react gate 25, ReactSignalSeam 6, upstream 121+1skip, build.sh, battery 25, LOC) against the final HEAD (super e54ac98, fork e21af85707) after confirming the workspace went quiet; all green. No evidence the churn was an attempt to game the judge — it looks like Round 2 finishing late. 2) STALE LIBRARY LOC SELF-COUNT: final REPORT.md section 3 still claims lib 1725 (core 603, runtime 298), but the final tree measures 1777 via the canonical counter (core 651, runtime 302) — the 14:47 perf commit added ~52 lines and section 3 was never refreshed even though the gates table was updated in the same final commit. Fork 188 is exact. This is the ranked objective-2 metric, so it counts as a (small, 3%) misreport; judged sloppiness, not gaming — the honest number is still ~2.7x smaller than the incumbents. 3) Minor: REPORT known-gaps section is commendably candid (draft-world fetch identity, epoch-based committed() approximation, untested async-action abort, direct atom.set bypassing the rebase log) — these are real gaps the next round should probe, especially the engine-API set() bypass during a live rebase episode.

## Notes

Design originality: genuinely its own stance. Incumbents copy state into worlds; fm1 keeps ONE canonical value-aware graph and makes speculation a REPLAY — per-atom rebase logs of write intents (set|fn) folded in call order (literal React updater-queue arithmetic), ephemeral Snapshot objects per render pass, and epoch-pinned history (atoms record past values only while >=1 snapshot pin is live; all scrubbed at zero pins). No identifier or structural overlap with cosignals-alt-a/alt-b/concurrent-solid-react src (different file layout, different vocabulary); isolation grep over both packages found zero incumbent imports (only REPORT.md mentions them for LOC comparison, which is required). Code quality: excellent — generous engineering-rationale comments, named types (Epoch, NodeVersion, TraceId, Lane), no line-golfing; counts survive prettier normalization (count-loc.mjs). Feature honesty spot audit: (a) lifetime effects GENUINE — engine test asserts open-on-first/close-on-last plus same-tick flap coalescing; React tests assert component-kind observation and StrictMode nets-one; union across kinds is architectural (subs set + external-observer count) though no single test mixes effect+component subscribers simultaneously (minor gap). (b) latest() context rule GENUINE at engine level — worlds.spec.ts:158 asserts latest inside a snapshot resolves that snapshot's world, not newer drafts, and render passes/battery use the same redirect mechanism; no direct latest()-inside-JSX-render assertion (weak edge). (c) mutation window GENUINE — gate-advanced 16 uses a real MutationObserver, asserts zero React-caused records across a committing write AND that a third-party appendChild is still caught. Ideas worth stealing: (1) the 188-line lane-facts seam — React exports only currentUpdateLane/currentRenderInfo/runWithPinnedLane + pass-start/pass-commit/mutation-window emits; batch identity and lane->batch mapping live wholly in userland; (2) pinned-lane commit folds (onPassCommit runs commitBatch under runWithPinnedLane so corrective renders on other roots land inside the owning batch's commit — satisfies binding ruling 2 with ~7 fork lines); (3) epoch-based per-root committed views (commitEpoch + valueAt(epoch), past values only recorded while pinned — quiescence holds nothing); (4) zero-alloc steady re-tracking via in-place dependency-prefix walk + frame-stamp dedupe. Adjudication: their documented dispute of the RULES prose updater example (adopting the calibrated battery's call-order replay, under which the prose example yields 3) is well-argued, pins both shapes in tests, and the battery passes — reasonable, not evasive. Benchmark honesty is a model: they report being ~1.7x slower than alien rather than hiding it.

## Re-judgement after fix round 2 (closing)

**Verdict: clean** · fork LOC 188 · lib LOC 1780

### Fixes verified

yes — (1) worlds.ts diff at ffcf890: suppression is per-atom (suppressLogFor: Atom|null) and one-shot (hook sets it to null on first match); consume-vs-cutoff order verified in core.ts Atom.set(): canonicalSetHook fires after writeGuard/materialize but BEFORE the equality cutoff and before subs.forEach(notifySub), so an equality-dropped setWithoutLogging still consumes the flag before returning, and the finally restores the saved prev unconditionally (covers writeGuard/materialize throws too) — no path leaks an armed flag to the next write; between arming and the hook only writeGuard/materialize run and initializers cannot set (initializing>0 throws), so no synchronous write can execute under a flag it doesn't own; nesting handled by prev save/restore. (2) Both judge probes print OK (probe: b=5 after retirement; probe2: control b=5, fold variant b=51 in call order). (3) All 4 new tests pass (cross-atom effect flush, same-atom write-back, retirement-fold mirror, watcher variant); fail-at-2a6ca9f spot-checked by reading git show 2a6ca9f:worlds.ts + write(): module-wide alreadyLogged stays true across the depth-0 synchronous effect flush inside atom.set(), the effect's urgent write(b,5) hits the hook's alreadyLogged early-return, b's rebase-log append is swallowed, and commitBatch(tb) replays draft 100 over urgent 5 — the first test's expect(5) fails on old code. (4) Oracle mirror-effect generation confirmed in diff (MirrorRule gen ~70%, acyclic dst!=src, model runs mirrors at construction and post-step with equality cutoff, engine registers matching effects); oracle passes at default seeds.

### Battery

25/25 pass. Fresh tamper check vs /Users/jitl/src/alien-signals-opt/royale/verify/battery: battery.spec.tsx, royale-types.ts, global.d.ts, vitest.config.ts, tsconfig.json, README.md all byte-identical; only package.json/pnpm-lock.yaml differ, solely by the required entry link (react-signals-royale-fm1 replacing the canonical placeholder cosignals-alt-b) plus lockfile fallout.

### Notes

Suites run sequentially, all green: engine 217/217 (4 files, incl. oracle at default 300 seeds), react gate 28/28 (5 files), battery 25/25, engine tsc clean, react-bindings tsc clean. Canonical LOC recount: forkLoc 188 / libLoc 1780, matching REPORT §9 exactly (188 unchanged, 1780 = +5 with worlds.ts the only source touched). Workspace quiet: no modified tracked files; only untracked royale/ROUND2.md and royale/verify-kit/ (the battery harness itself plus round notes). Re-entry audit holds as claimed: effects and watchers drain via the same sink queue after the hook consumed the flag; lifetime onObserved is microtask-deferred (verified comment/test claim in worlds.spec.ts watcher test); retirement folds arm per-fold with prev restore. No residual leak class found in the exact round-1 regression family. Closes fm1 as clean.
