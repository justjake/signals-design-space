# Judgement scorecard: fm2

**Verdict: clean** · fork LOC 48 · lib LOC 1734

## Gates (claimed vs observed)

All gates independently re-run at HEAD d1e8fe2 (fork 0f4dab6b1a), workspace quiet throughout, no mid-judgement commits. Typecheck engine PASS, react PASS (tsc strict, includes src+tests+royale). Conformance 179/179 ("Tests 179 passed (179)"); judge-instrumented rerun printed TOTAL=179 SKIPS=0 — untracked/batch/effectCleanup/computedThrows all genuinely implemented, no silent SkipTest. Engine suite 204/204 (316ms). Oracle deep sweep ORACLE_SEEDS=1200 PASS ("Tests 1 passed", 712ms); judge negative control (mutated model fold in a copy) failed seed 1 with shrunk 2-op schedule — harness genuinely detects divergence. Leak audit 5/5 (pool:'forks' + --expose-gc confirmed in vitest.config.ts). Fork: `git am` of patches/ onto pristine e71a6393e6 worktree applied cleanly; `git diff HEAD royale/fm2-react` empty (patch series exactly regenerates the branch); build.sh succeeded ("Built: 19.3.0 (0f4dab6b1a)"). Fork protocol jest: 6 passed, 6 total. Upstream adjacency packages/react-reconciler: 76 passed/76 suites, 1140 passed, 19 skipped. yarn prettier-check clean. Entrant real-React gate: 31/31 (in-package battery = canonical battery modulo header comment + import wiring, 8 diff lines, + 6 hooks tests). Shared verify-kit battery: 25/25 ("Test Files 1 passed, Tests 25 passed"); kit tamper check: battery.spec.tsx, royale-types.ts, vitest.config.ts, global.d.ts all byte-IDENTICAL to canonical; ADAPTER.ts + package.json wiring-only. Canonical count-loc.mjs: forkLoc 48, libLoc 1734 — matches self-report exactly. milomg: frameworksList registers with testPullCounts:true; 4/4 Royale FM2 adapter tests pass incl. exact pull counts (sole clone failure is pre-existing "x-reactivity", disclosed in REPORT); isolated runner 1-round spot run consistent with REPORT table (propagation ~1.1-2.3x alien, creation ~5x, geomean ~2x). react-bench reproduces same shape under shared-machine load (fanout 3.6 vs claimed 2.0, transition p95 16.5 vs 12.9, mount 64 vs 59.5 — direction and honesty confirmed; their transition numbers are reported against self-interest with a fair de-opt caveat).

## Shared battery

25/25 shared verify-kit battery (byte-identical kit, wiring-only ADAPTER); 31/31 in-package real-React gate (faithful battery copy + hooks specs). Judge probes beyond the battery: latest() context rule PASS at engine level (canonical computed evaluation sees canonical, world evaluation sees its batch draft) AND in real-React renders (urgent pass during a pending transition does not see the draft through latest(); outside render latest()=newest intent while read()=canonical) — the exact failure mode of two prior entries, genuinely handled. Lifetime effects PASS probe: union across subscriber kinds holds one observation, within-tick flap coalesces to nothing, last-of-every-kind cleanup fires. Mutation window PASS probe: MutationObserver disconnect-on-start/reconnect-on-stop sees zero React-caused records on a mutating commit while catching third-party layout-effect mutations (window excludes layout/passive phases); fully-bailed-out commit emits no window (pinned in fork tests).

## Red flags

No gaming found. Minor notes only: (1) leftover debug scratch (debug7.spec.tsx, vitest.debug.config.ts) inside royale/verify-kit/battery — excluded by the canonical vitest config, no effect on the gate, just clutter. (2) On a commit where React runs its mutation phase but performs no actual DOM writes (React 19 props-identity Update flag with unchanged output), the window still fires — compliant (it brackets React's real mutation phase and the blind observer misses zero records), but a stricter reading of "no window when nothing mutates" only holds for full bailouts; judged not a violation. (3) The dispatch-stance mentioned "uSES for the urgent path" but the shipped hooks use a useReducer bump counter + engine re-read, not useSyncExternalStore — a design drift from the stance, not a rules issue, and arguably the better design. (4) Engine writes issued inside an async transition after an await (outside startTransitionWrite) would classify urgent while React would render them at a transition lane — outside gate scenarios; adjacent coarseness (lane collisions, per-lane batch lingering on abandoned roots) is candidly disclosed in REPORT known-gaps. (5) ROUND2's verbatim esbuild command lacks --platform=node; their committed dist was correctly built (adding the flag reproduces it) — cosmetic.

## Notes

Strong entry; every REPORT claim verified honest, several against self-interest (transition bench slower than stock baseline, reported with a defensible caveat; creation suites ~5x alien disclosed). The 48-line fork is real and delivers both binding rulings: mutation events bracket exactly commitMutationEffects inside React's own MutationMask guard, and royaleRunWithLane pins requestUpdateLane so corrective re-renders join the owning transition's commit (fork test pins pin+unwind; battery scenario 5 exercises mount-mid-transition). royaleProbeLane mirrors React's own requestCurrentTransition/requestTransitionLane classification, so passive-effect and post-timeout writes agree with what React itself would do. The README justification ledger exists, covers all 48 lines in 6 groups, and its capability arguments hold (no public API exposes render-pass lanes — the root cause of the uSES de-opt). Net-change (revert-aware, Check-not-Dirty) validation does not fudge pull counts: 179/179 with judge-verified zero skips plus milomg testPullCounts:true passing. Isolation clean: no incumbent references; the engine (world-overlay + per-atom dispatch-ordered write queue mirroring React's updater queue, string-keyed world caches, in-place zero-alloc dep re-tracking) is recognizably its own design. Ideas worth stealing: (a) probe/pin lane pair as the entire concurrency seam; (b) committed views recorded from layout effects so only commit-surviving renders record — per-root screens at zero write-path cost; (c) values-never-in-React-state bump-counter hooks that let React's updater-queue replay supply rebase arithmetic; (d) canonical fast path completely free of world bookkeeping. Readability high, comments are engineering rationale, no golfing (counts are post-prettier). Fork 48 vs incumbent 1510; lib 1734 vs 4689/4909 — best-in-class sizes if peers verify similarly.
