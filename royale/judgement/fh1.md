# Judgement scorecard: fh1

**Verdict: issues** · fork LOC 167 · lib LOC 1941

## Gates (claimed vs observed)

All gates re-run independently at HEAD dd880bb (fork 1492d0d57c), all green: typecheck engine+react clean (tsc exit 0 both). Conformance 179/179 (207ms). Oracle default 300 seeds green; deep ROYALE_FUZZ_SEEDS=1200 green ("oracle fuzz (1200 seeds x 90 steps) ... 128ms" — genuine 418-line naive-model fuzz, not gamed). Engine full suite 206/206. Leak audit 7/7 (forks pool, --expose-gc; asserts internal hist/log/draftSubs/wc nulls + WeakRef collection). Real-React gate 19/19. Fork protocol: 2 suites, 9/9 (ReactFiberSignalSeam, ReactDOMSignalSeamMutation). Upstream adjacent: 13 suites, 121 passed 1 skipped (pre-existing gate skip). Flow dom-node: "No errors! Flow passed for the dom-node renderer". Pristine fork: git am of patches/ onto base e71a6393e6 in fresh worktree applies cleanly, 0-line diff vs royale/fh1-react; rebuilt via fork/build-react.sh ("Built: 19.3.0 (1492d0d57c)") and re-ran real-React gate (19/19) + shared battery (25/25) against the fresh build. Tamper check: battery.spec.tsx, royale-types.ts, vitest.config.ts, global.d.ts byte-identical to canonical; ADAPTER.ts and package.json wiring-only (entry link swapped for incumbent link); count-loc.mjs identical. Fork numstat cross-check: 107+53+7=167, only 2 fork test files excluded (legitimate). Benchmarks spot-checked: milomg registration wiring-only (3 touch points), adapter sanity 79/80 with the 1 failure being the pre-existing upstream x-reactivity adapter exactly as disclosed; 1-round isolated run consistent with claimed 1.95x alien geomean; react-bench transition scenario reproduces: p95 5.368ms vs baseline 406.780ms (claimed 5.808 vs 403.267).

## Shared battery

25/25 pass (re-run against a fork build I rebuilt from the patch series myself). Tamper check clean: all four battery files byte-identical to the canonical verify kit; ADAPTER.ts is a one-line re-export of the entry adapter; package.json diff is link-target wiring only. Note: the battery only probes latest() at TOP level (scenario 2, line 162) — it does not exercise the latest()-in-context rule, so 25/25 does not contradict the context-rule failure I found by direct probe.

## Red flags

1) latest() context rule VIOLATED (the flagged discriminator — probed independently, both contexts): (i) inside a CANONICAL computed evaluation, latest() folds in ALL live draft batches — a computed first-evaluated (or created) while a transition draft is live caches a draft-derived value visible to canonical readers (probe: read(c)=20 while read(a)=1 with draft a=2 live); (ii) latest() inside a canonical computed evaluation is UNTRACKED — the computed goes permanently stale (probe: after urgent a=2, a=3 and full quiescence, read(c) still served the original 10; effects watching it never re-fire); (iii) in a RENDER pass, a direct latest() call in a component body during an urgent re-render (whose world is canonical — DOM showed v:1) returned the live transition draft (2), a read-ahead tear beside useValue in the same pass. Works correctly in world-scoped computed evaluations, inWorld scopes, and all hook-mediated reads; engine.ts:1455-1458 docstring and REPORT §5 ("Read family ... — done") claim the full rule, and this gap is NOT in the known-gaps section — inaccurate feature claim, though plausibly believed (the activeWorld machinery exists and the oracle/battery don't model this case). 2) Stale LOC self-count: REPORT claims libLoc 1876 (engine.ts 1246); canonical counter at HEAD gives 1941 (engine.ts 1311). I verified 1876 is the count at pre-perf commit 1c3ca8e — the Round-2 perf commit (9df7031, +65 lines) landed before the REPORT commit but the count was not re-run. Understates by 3.5%; sloppy, not gaming (fork 167 is exact; entry remains ~2.4x smaller than incumbents). 3) Minor: handle.errors is a dead channel (disclosed honestly in REPORT). No isolation violations: zero incumbent imports, no copied incumbent/alien-signals code found; milomg changes registration-only.

## Notes

Feature spot audits: (a) lifetime effects GENUINE — my engine probe confirmed one observation across the union of kinds (computed-chain, engine effect, hook subscriber), subscribe/unsubscribe flap within a tick nets zero callbacks, StrictMode nets one (entry test + battery). (b) latest() context rule FAILS — see red flags; this entry joins the failing pair on that discriminator. (c) mutation window GENUINE — fork emits onMutation(true/false) bracketing exactly commitMutationEffects+resetAfterCommit inside flushMutationEffects (layout/passive effects outside); entry test uses a real MutationObserver with disconnect-on-start/reconnect-on-stop, asserts zero leaked React records AND a positive control that third-party appendChild IS observed; fork jest test additionally pins "no bracket for a commit with no host mutations". Entry-specific: (a) quiescence self-destruction GENUINE — probe confirmed hist/log/episodeBase/draftSubs all reclaimed on BOTH retire and discard paths; gc test asserts internals plus WeakRef collection; retention is gated on liveBatches empty AND openWorlds==0 (a host that leaks a World would pin histories, but the seam releases pass worlds at commit and onPassStart). (b) seam narrowness CONFIRMED — exactly 4 fact callbacks (onPassStart/onRootUpdated/onCommit/onMutation) + 2 queries (getWriteLane, getRenderContainer) + 1 control (pinnedTransitionLane); the mutation window is a fact callback, not a query, so the narrow-seam claim holds; corrective re-renders use pinnedTransitionLane around React.startTransition in runInBatch so the join-fixup commits inside the owning batch's lane (fork test pins the lane routing; battery scenario 5 passes). (c) rebase direction AGREES with the calibrated battery — urgent-alone applies to base (1*2=2), retirement refolds the whole interleaved log in scheduling order ((1+1)*2=4), and a constant draft recorded before an urgent update folds in call order rather than clobbering (probe: set(10) draft then urgent +5 retires to 15). Originality/stance: the version-stamped-visibility design is real and recognizably distinct — no world tables or overlay stores anywhere; worlds are (cutoff, batch-list) predicates folded over per-signal logs, per-root committed views are just latched cutoffs. Ideas worth stealing: the 107-line ReactFiberSignalSeam.js published on ReactSharedInternals with absence-as-feature-detection; the pinnedTransitionLane one-field control (vs heavier incumbent mechanisms); episode-scoped histories that make quiescence-costs-nothing structural rather than a cleanup pass; the orphan-batch double-microtask probe. Readability is strong: generously documented with engineering rationale, named number types, no line-golfing (1941 lines includes real doc comments). Known-gaps section is unusually candid. Verdict rationale: all claimed gates verified green and honestly reported; the two real dings are the undisclosed latest()-context failure (a required-feature defect misreported as done) and the stale lib LOC — issues for ranking, not disqualification-level gaming.
