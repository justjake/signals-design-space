# Judgement scorecard: fx2

> Historical naming: `signals-royale-fx2` is now named `cosignals`.

**Verdict: clean (after fix round; originally issues)** · final fork LOC 11 · final lib LOC 2239 · fork LOC 11 · lib LOC 2194

## Gates (claimed vs observed)

QUIET: last commit 17:26:55, waited past 3-min window; no new commits; no live build on the entry workspace.
TYPECHECK: engine tsc --noEmit exit 0; react pkg exit 0.
CONFORMANCE: 179 passed (179).
ENGINE SUITE: 221 passed (221) across 6 files (worlds 15, features 11, asyncs 8, oracle 2, conformance 179, gc-leaks 6).
ORACLE: default 300 seeds green; deep sweep ROYALE_FX2_SEEDS=1200 green ('oracle fuzz (1200 seeds x 90 steps)'); source audited — real seed loop, genuine naive replay model (per-cell intent logs, world folds, memo-free rederivation), greedy shrinker, sabotage canary really stubs ri.retireDraft and demands detection.
LEAK AUDIT: gc-leaks 6/6 under pool:'forks' + --expose-gc (verified in vitest.config.ts); includes quiescence and the scope-owned-effect-survives-GC regression.
REAL-REACT GATE: 28 passed (28) (real-react 13, features-react 12, host-guarantees 3) — re-run again after pristine rebuild: 28/28.
FORK JEST: ReactDOMFx2MutationWindow Tests: 6 passed, 6 total (handshake marker, strict per-commit bracketing, mutations-only-inside-window with layout/passive after stop, per-root containers multi-root, EMPTY window fires for mutation-less commits, stock-unchanged when hook uninstalled).
ADJACENT UPSTREAM: ReactDOMRoot+ReactFlushSync+ReactTransition = 70 passed, 1 skipped (upstream's).
PRISTINE PATCH REPLAY: fresh worktree at e71a6393e6, git am patches/0001 → tree 6d72a7a9b5d23180aaf5807a2aba36b666cd124b == royale/fx2-react tree — BIT-IDENTICAL. (REPORT quotes '30b03c73…' — that hash is some other tree object, not the branch tree; substance verified, quoted hash inaccurate.) build.sh rebuilt (Built: 19.3.0 (3e917ac76b)); react gate 28/28 and battery 25/25 re-run green on the fresh build.
LOC: canonical count-loc.mjs → forkLoc 11, libLoc 2194 (REPORT said 2193; graph.ts 727 vs 726 — trivial). Fork numstat independently: 11/0, one file (ReactFiberWorkLoop.js) + 175-line jest test (excluded). Read the whole diff: 5 lines at flushMutationEffects entry (3 comments + hook read + start call), 2 at mutation-loop exit before root.current swap/layout phase (blank + stop call), 4 at EOF (blank + 2 comments + marker assignment). REPORT ledger splits 6/2/3 — misdistributed, total exact.
MILOMG SPOT CHECK: rebuilt isolated bundle from current src (entrant's ROUND2 esbuild command actually errors without --platform=node; added flag), 1 round Royale FX2 vs Alien Signals → geomean 1.339 (stale-bundle run: 1.172); direction fully corroborates claimed 1.278 3-round median (behind alien, ~1.2-1.5x on propagation family, unstable ~2.5x worst as disclosed). Adapter sanity: Royale FX2 4/4 incl. pull counts; only failure is x-reactivity 51-vs-41 — pre-existing upstream exactly as REPORT discloses.
REACT-BENCH SPOT CHECK (transition scenario, both contenders): royale-fx2 p95 9.99ms / max 10.03ms / completed 1798ms; uses-baseline p95 3.40ms / max 367.27ms / completed 1466ms — headline (10ms vs ~330ms max urgent) corroborated. Urgent input confirmed as real dispatched MouseEvent clicks. Baseline is a fair idiomatic ~35-line uSES store, same shapes.
INDEPENDENT PROBES (9/9 in scratch harness /tmp/fx2-probes against entry adapter + fresh fork build): (a) latest() context rule GENUINE — urgent passes pair latest===useValue, transition pass sees (2,2), ambient latest sees draft while canonical read doesn't, canonical computed evaluates canonical (100) while held AND recomputes after fold (200 — tracked, no permanent staleness). (b) refresh-in-transition with UNCHANGED inputs GENUINE at engine level — hidden nonce draft-op forces the in-world refetch (fetchCount 1→2, new request created, stale serves, canonical unchanged, refetch commits with the transition, no double fetch); ONE GAP: useIsPending hook never flips during the transition-owned refetch (engine isPending(data)=true, rendered hook stays false — draft nonce append doesn't notify pending probes; undisclosed). (c) mutation window GENUINE — MutationObserver blinded via disconnect-on-start sees 0 React records, third-party mutations caught, empty-commit window fires (contract pinned in fork jest too). (d) lifetime effects GENUINE — one observation across engine-effect + computed-chain + React component union; last-unsubscribe cleanup; dispose+resubscribe flap within a tick coalesces (0 cleanups). SIGNALSCOPE EDGES: (i) scope-less-only root: drafts classify and retire via microtask path, readers repaired — OK. (i-b) MIXED roots BUG: with one scoped root holding+committing a transition, a subscriber in a bare (scope-less) root shows stale value after the silent fold (b:1 while canonical=2), repaired only by the next canonical write on that cell — the silent retire suppresses reactEpoch and pokeLeafObservers wakes only committed-view watchers, so epoch-snapshot subscribers outside any scope bail forever. Undisclosed; out-of-documented-shape (README/wrapCreateRoot auto-install the scope) but constructible from public exports. (iii) StrictMode: transition update nets exactly once; [T:*2,U:+1] from 2 → urgent 3 → fold 5, matching battery's scheduling-order replay semantics, no double replay. (iv) time-slicing: battery scenario 12 green on pristine build; bench corroborates no sync de-opt. DISCLOSED GAP CONFIRMED REAL AND AS DESCRIBED: plain latest() in an urgent pass with no fx2 hook earlier resolved the previous pass's world (sampled 2, canonical answer 1) — honest disclosure verified.

## Shared battery

TAMPER CHECK CLEAN: battery.spec.tsx, royale-types.ts, vitest.config.ts, global.d.ts all byte-identical to /Users/jitl/src/alien-signals-opt/royale/verify/battery/ (cmp). ADAPTER.ts is a single re-export of the entry's royale/adapter.ts (wiring only). package.json diff is wiring-only: swaps cosignals-alt-b link for react-signals-royale-fx2 link; react/react-dom/scheduler still link to the entry's vendor/react fork build. RESULT: 25/25 passed, run on the freshly rebuilt fork (post-pristine-replay build), 619ms. Scenario 12 (time slicing, real scheduler, no act) included and green.

## Red flags

None disqualifying. Findings, ranked: (1) UNDISCLOSED BUG (moderate, edge-shape): a useValue subscriber in a root WITHOUT SignalScope goes stale after a transition retires via another (scoped) root's commit — silent fold suppresses the reactEpoch snapshot and the poke only wakes committed-view watchers; stale until the next canonical write on that cell. Probe-pinned (probes.spec.tsx E i-b). Documented usage (wrapCreateRoot) always installs the scope, and every judged shape passes, but SignalScope is a public export and the gap is not in REPORT §8. (2) UNDISCLOSED GAP (minor): useIsPending never flips during a transition-owned refresh with unchanged inputs — engine isPending is true but the draft-side nonce append doesn't notify pending-probe subscribers; urgent refresh and plain transition writes flip correctly (battery-pinned). (3) REPORT imprecisions (trivial): quoted patched-tree hash 30b03c73 is not the actual tree hash (6d72a7a9, which I verified bit-identical both sides); libLoc 2193 vs 2194 actual; fork ledger line-split 6/2/3 vs actual 5/2/4 (total 11 exact). (4) Handshake ledger slightly overstates impossibility of markerless detection (a flushSync probe-commit at registration is synchronous), though the marker remains a reasonable choice — noted for LOC-comparison fairness against any zero-marker entry. (5) Global protocol carrier (**FX2_MUTATION_WINDOW**) is last-writer-wins across duplicate library instances — silent subscriber loss in a two-copy page; theoretical in this tournament's shapes.

## Notes

IMPOSSIBILITY LEDGER VERDICT: Entry edge SOUND — nothing in stock React gives a synchronous, React-attributable pre-mutation edge (getSnapshotBeforeUpdate is class-fiber/update-gated, MutationObserver is async-after-the-fact, DOM prototype patching cannot attribute React vs third-party). Exit edge SOUND — stop must land after the last host mutation and before root.current swap/layout; diff placement verified correct. Handshake MOSTLY SOUND but slightly overstated: their 'cannot detect except by triggering a commit and waiting' ignores that a registration-time probe could flushSync-render a detached root and check the hook fired synchronously — no waiting. That probe needs DOM + react-dom and a side-effect commit, so the 3-line marker is a defensible engineering choice (loud failure in SSR/non-DOM too), but a ~8-line fork was arguably possible. DISPATCH: uninstalled cost is one globalThis property read + two null checks per commit — accurate; 'stock unchanged' pinned behaviorally by fork test. Global-carrier caveats (noted, not gating): a second copy of the bindings in one page overwrites **FX2_MUTATION_WINDOW** (first instance's onDomMutation subscribers go silent — no cross-instance handshake); a throw inside the mutation loop would leave the window open (mostly theoretical — React captures commit-phase errors per-fiber); test isolation fine (forks pool, per-process globals). LIBRARY INTERNALS CHARACTERIZATION: read-only access to React.\_\_CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE (.H live hook dispatcher for are-we-rendering + write-during-render guard; .T current transition object for ambient write classification keyed in a WeakMap) — same legal category as fm2; NO fiber traversal, NO prototype patching, NO runtime patching of React; engine package has zero imports and zero deps. No hidden fork machinery found. ISOLATION: clean — no incumbent imports, no _\_cosignal_/concurrent-solid references; design (worlds as SignalScope reducer state riding React's own update queues; uSES snapshot = subscription epoch, never a value; silent fold + committed-view poke) is recognizably distinct from the incumbents' 1510-line fork approach. ORIGINALITY/READABILITY: high — module-level design essays, engineering-rationale comments, house style (named number types, create-not-mint) followed; no line-golfing (comments plentiful; count survives prettier normalization). IDEAS WORTH STEALING: (1) subscription-epoch-as-uSES-snapshot to keep transitions un-de-opted; (2) draft ids dispatched into a per-root useReducer inside startTransition so React's updater queues ARE the world bookkeeping (rebase/replay for free); (3) refresh() as a hidden always-tracked nonce cell routed through write classification; (4) effect ownership rule for FinalizationRegistry arming (only ownerless effects arm); (5) revision-counter re-dispatch for drafts appended after a pass rendered them. HONESTY: every gate claim reproduced; benchmark disclosures (behind-alien geomean, unstable 2.96x, shared machine, x-reactivity upstream failure) all check out; three trivial inaccuracies (quoted patched-tree hash, libLoc off-by-one, ledger line distribution 6/2/3 vs 5/2/4). VERDICT RATIONALE: 'issues' for two narrow undisclosed correctness gaps found by probing — (A) mixed scoped/bare-root subscribers miss silent transition folds (stale until next canonical write; out-of-documented-shape but constructible), (B) useIsPending doesn't flip during a transition-owned refresh (engine-level correct, hook delivery missing). Neither hits a required gate scenario, both look like unknown edges rather than papered-over failures; honesty gate intact. Everything headline — the 11-line fork, pristine replay, 179/179, 25/25, the milomg ratio direction, and the 10ms-vs-330ms transition story — is real.

## Re-judgement after fix round (closing — final verdict of the tournament)

**Verdict: clean** · fork LOC 11 · lib LOC 2239

### Fixes verified

Both fixes verified. (1) Mixed-root staleness: canonicalEpoch companion counter bumps on every canonical change including silent folds (graph.ts mark/writeCell/invalidateDerived); useValue snapshots it only when world === UNSCOPED_WORLD sentinel — scoped subscribers can never take that path (SignalScope reducer seeds EMPTY_WORLD, a distinct identity, and only produces fresh objects), so render-pass isolation and the no-repair-storm property hold. draftAudience gate correct on all edges: mid-transition-mounted scope (in providers, not audience) forces a loud fold; mid-transition-unmounted scope retires loud if last recipient (no hang) and never blocks silence otherwise; missing audience defaults loud. (2) pokeLeafObservers walks watched derived edges with a seen-set (cycle-safe); effects (onNotify undefined) never poked. All 4 regressions pass post-fix; fail-at-pre-fix empirically confirmed for the poke regression by running it in a 537e4c8~1 worktree (probe on computed never wakes). Judge probes 8/9 with the sole failure being probes.spec.tsx:378's pinned b:1 staleness expectation now receiving the correct converged b:2 — that failure IS the fix.

### Battery

25/25, spec/types/config byte-identical to /Users/jitl/src/alien-signals-opt/royale/verify/battery (cmp clean; only ADAPTER.ts + package metadata differ, expected). Engine 224/224; react gate 31/31; oracle green at default 300 and 1200 seeds with both canaries (sabotaged-engine, blinded-snapshot) and the bare-subscriber fold class present; both typechecks clean; fork bit-untouched (clean working tree, tree 6d72a7a9b5d2..., product numstat 11).

### Notes

Canonical counter: fork 11 / lib 2239, matching REPORT §4/§10.4 exactly (2194→2239, +45). §10.3 trivia corrections present (ledger 5/2/4, patched tree 6d72a7a9, judged-commit libLoc 2194); §8 gained both the **FX2_MUTATION_WINDOW** global-carrier caveat (last-writer-wins under duplicate bundles) and the markerless flushSync-probe note with rationale for keeping the 3 handshake lines. Suites run sequentially as instructed. No modifications made to the entry (read-only worktree at 537e4c8~1 used for the pre-fix check, then removed). This closes fx2 clean and ends the judgement phase.
