# Judgement scorecard: fh2

**Verdict: clean** · fork LOC 211 · lib LOC 2740

## Gates (claimed vs observed)

All claimed gates reproduced by me at HEAD 1962bc1 (fork gitlink ace2536b28; workspace quiet). Typecheck: engine PASS (tsc strict, exit 0), react PASS. Engine suite: 6 files, 216/216 pass; conformance verbose run shows exactly 179/179, 0 skipped. Oracle fuzz: default 300x90 inside suite; deep ORACLE_SEEDS=1200 re-run green (loop verified genuine: per-seed generate/run/shrink). Leak audits: engine gc-leaks.spec.ts + react gc-leaks.spec.tsx pass under pool:'forks' + --expose-gc with real WeakRef/gc assertions and quiescence internals check ({openBatches:0, worldCaches:0, pendingListeners:0}). Real-React gate: 3 files, 30/30 on their fork build — re-ran again after I rebuilt the fork from source (still 30/30). Fork protocol: yarn test ReactDOMExternalSignals 8/8. Upstream adjacents: ReactDOMRoot|ReactFlushSync|ReactTransition|ReactStartTransition = 7 suites, 72 passed + 1 skipped; ReactFiberHostContext|ReactMutableSource|useSyncExternalStore = 5 suites, 27 passed. Total 99 passed/1 skipped, matching claim. Pristine fork build: git worktree at base e71a6393e6 + git am both patches → tree e47ac9ea... byte-identical to royale/fh2-react HEAD tree; ./fork/build-react.sh rebuilt clean ("Built: 19.3.0 (ace2536b28)"). Shared battery: tamper check PASS (battery.spec.tsx, royale-types.ts, vitest.config.ts, global.d.ts byte-identical to canonical; ADAPTER.ts is a one-line re-export; package.json wiring-only links) → pnpm install + pnpm test = 25/25 against the freshly rebuilt fork. Milomg: registration + adapter verified (testPullCounts:true, scoped build + cleanup() disposes scope — no leak asymmetry); my own 1-round isolated run matches the claimed ~1.28x profile (cellx2500 2.4x worst, 25-1000x5 ~parity, most suites 1.2-1.3x).

## Shared battery

25/25

## Red flags

No disqualifying flags. Minor notes: (1) libLoc self-count says 2733; canonical counter says 2740 — 7-line drift, commands match, not gaming. (2) graph.ts's propagation core (propagate/checkDirty/link/unlink/shallowPropagate, flags scheme) is a near-verbatim port of upstream alien-signals v3 system.ts, not merely 'alien-shaped' — it IS attributed in the file header ('The algorithm follows alien-signals v3 (MIT)') and alien-derived algorithms are explicitly legal, but the originality ledger should record ~250 of graph.ts's 677 lines as a direct attributed port. No verbatim copying from the incumbents found: zero incumbent imports/references; the engine layer's per-atom insertion-ordered op-queue + world-fold design shares only generic vocabulary (readInWorld) with alt-b, whose architecture (WorldSpec/resolveNode) is structurally different. (3) Fork's 306-line **tests** file is genuinely tests (8 its, no product logic), correctly excluded. No line-golfing (prettier-normalized count, no >130-char lines), no 'mint' vocabulary, adapters are thin wiring (23-155 lines).

## Notes

Feature-honesty spot audits all GENUINE. (a) Lifetime effects: real onWatched/onUnwatched seam + microtask debounce; their tests cover React-subscriber union and engine-effect flap coalescing; my own engine probe additionally confirmed the union includes a two-level computed-chain subscriber (observe on effect attach, unobserve on dispose). (b) latest() context rule: verified by my own probes since their tests don't cover it directly — inside a computed evaluation with a live draft (canonical 10, draft 99): canonical-world eval sees 10, draft-world eval sees 99; my React-level probe (scratch package against their fork build) confirmed latest() inside an urgent render during a held transition returns the render world's 10, never the draft 99, then 99 after retirement. (c) Mutation window: features.spec scenario 16 is a real MutationObserver disconnect-on-start/reconnect-on-stop test asserting zero leaked React mutations, paired start/stop edges per container, and third-party mutations still caught; the fork brackets flushMutationEffects exactly (start before mutation phase, stop before root.current swap, 'committed' before layout). ENTRY-SPECIFIC: (a) Stance drift is smaller than the scout suggested — the assigned fiber-granular stance survived intact at the React layer: the fork exposes render identity (pass start/discard, commit-phase triples, lanes) and each component instance is its own engine subscriber with a per-instance useReducer forcer dispatched under runWithLane (per-component-instance corrections, verbatim the assignment). The drift is in the engine underneath: canonical alien-v3 graph + per-atom draft operation queues + on-demand world folds (the two-layer split the scout saw). Ledger: assigned stance kept at the seam/bindings; engine architecture is the entry's own draft-overlay design; graph core is an attributed alien-v3 port. (b) Size: 2740 is the field's largest lib but buys real substance — full alien-v3 graph for conformance/perf (677), async/suspense layer with thenable identity + settled history, world caches, per-root committed views, SSR, tracer (152+38, a required feature so correctly counted), and it pairs with the field's smallest fork (211 vs incumbent 1510). No padding found; comments are extensive but free per rules. Honest tradeoff: 1.28x geomean behind alien (incumbents claim parity), stated candidly in REPORT with a credible profiling explanation (node size/per-op constants, identical checkDirty visit counts). (c) Updater-queue rebase: battery scenario 3 (1+1)*2=4 and scenario 13 (1+2)*2=6 both pass (25/25); their worlds.spec.ts:46 pins exactly the battery's adjudicated insertion-order ('scheduling order') replay; my engine-level probes reproduced both shapes including urgent-alone intermediate values (2/2) and never-3/never-4. Note for adjudicators: RULES.md's prose example ('transition \*2, urgent +1 → 4') is inconsistent with insertion-order replay (would give 3); the battery's calibrated semantics is what fh2 implements — not an entry defect. Ideas worth stealing: the 136-line single-slot injectable seam (inject/runWithLane/currentTransitionLane/scheduleRootLane + isRenderPhase reuse); batch-lifetime-IS-lane-lifetime so quiescence reclaims episodic state by construction; scheduleExternalRootLane as a cheap close-edge for write-only batches; per-atom insertion-ordered op queue with prefix compaction as a natural React-updater-queue rebase; worldStamp-keyed snapshot caching in the uSES store. Readability: high — well-factored, house style followed, engineering-rationale comments.
