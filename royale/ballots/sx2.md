## judge packages/signals-royale-fh1

> Historical naming: `signals-royale-fx2` is now named `cosignals`.
> score out of 10: 9.1
> favorite thing: Predicate worlds over version-stamped histories express render and committed views without overlay stores while keeping the fork to 167 lines.
> least favorite thing: The resumed-pass unsubscribed-atom caveat and roughly 1.95x core slowdown show that the elegant model still has a consistency edge and a material steady-state cost.
> something you learned: A root's committed view can be represented as a history cutoff rather than a copied store when episode histories are reclaimed at quiescence.

## judge packages/signals-royale-fh2

score out of 10: 9.3
favorite thing: The fiber-granular delivery design combines a complete 25/25 result with the field's strongest measured core balance at about 1.28x Alien.
least favorite thing: The 2,733-line library and per-component uSES-plus-forcer subscription tax are a large price for that precision.
something you learned: Canonical delivery can ride useSyncExternalStore while draft delivery uses a lane-pinned reducer without forcing the speculative value into the external-store snapshot.

## judge packages/signals-royale-fm1

score out of 10: 8.8
favorite thing: The value-aware polling graph and replay snapshots fit the complete behavior into a relatively compact 1,780-line library with credible fuzz coverage.
least favorite thing: The epoch-based committed view is only exact while histories remain pinned, and React abandonment still has no explicit discard edge.
something you learned: A direct atom set must enter the rebase log before synchronous effects flush, and suppression of replay logging must be one-shot and per atom.

## judge packages/signals-royale-fm2

score out of 10: 8.9
favorite thing: The 48-line inert host protocol is the cleanest full scheduling seam in the field and leaves batch semantics entirely in userland.
least favorite thing: A permanently abandoned root transition can retain a live batch, and every successful transition pays a value-equal retirement repair render.
something you learned: Layout effects can record per-root screen truth precisely because suspended and discarded renders never execute them.

## judge packages/signals-royale-fx1

score out of 10: 9.1
favorite thing: Store-owned episodes with transition-object lane pinning produce excellent urgent latency through an unusually small 80-line fork.
least favorite thing: The 2,343-line engine carries substantial episode, frame, root, and async-context machinery, with topology-based pending checks that knowingly over-report.
something you learned: Claiming a lane once on the transition object lets original deliveries, late joins, and owned settlements share one React commit.

## judge packages/signals-royale-fx2

score out of 10: 8.7
favorite thing: Carrying draft identities through ordinary React state reduces the fork to an exceptional 11 mutation-window lines while retaining near-Alien core performance.
least favorite thing: Plain latest() in a render before any fx2 hook can still resolve the previous pass's world, so a required contextual-read rule remains incomplete.
something you learned: React's own reducer queues can transport an external world's identity, leaving only the exact DOM mutation bracket fundamentally unavailable to userland.

## judge packages/signals-royale-sh1

score out of 10: 9.0
favorite thing: A complete battery, 94-line fork, and 1,217-line combined library make this the strongest small-code correctness result.
least favorite thing: The approximately 3.93x core slowdown and extra-request risk for inline fresh promises are significant costs hidden behind the compact source count.
something you learned: Root-owned write and rebase sets can repair late mounts and pending flips without turning a transactional engine into a global broadcast system.

## judge packages/signals-royale-sh2

score out of 10: 7.5
favorite thing: The typed node and intrusive edge slabs are a serious allocation-conscious attempt rather than another object-graph variation.
least favorite thing: Permanent batch abandonment, committed computed views, and general transformed async evaluation remain partial, so the entry does not meet its own complete-correctness standard.
something you learned: Numeric handles and typed slabs can make atom creation extremely cheap without automatically making computed evaluation or propagation competitive.

## judge packages/signals-royale-sm1

score out of 10: 7.4
favorite thing: The final single sequence-ordered operation log is semantically direct, fuzzed deeply, and honestly corrects the earlier replay-order implementation and reporting error.
least favorite thing: A 320-line fork and roughly 6.55x overall core slowdown make this one of the field's most expensive complete implementations.
something you learned: An event-closure signal is enough to retire external-only lanes that never acquire an owning root.

## judge packages/signals-royale-sm2

score out of 10: 8.5
favorite thing: The fork rewrite removed 1,324 lines of React-side machinery while preserving a clean 25/25 result and exact lane-root retirement behavior.
least favorite thing: Capsule propagation remains about 2.99x Alien and the post-rewrite React mount and fanout measurements are materially behind the plain store.
something you learned: Identifying an engine batch directly with its React lane can collapse token, registry, and translation layers without sacrificing multi-root retirement.

## judge packages/signals-royale-sx1

score out of 10: 6.8
favorite thing: The 1,295-line library packs ordered world folds, weak ownership, per-source delivery, and full shared-battery behavior into a compact userland implementation.
least favorite thing: The 476-line fork is the field's largest and Cellx remained CPU-bound for over fifteen minutes, while fresh-promise identity and deterministic interruption stayed partial.
something you learned: Whether an urgent operation occurs before or after a deferred world renders can be encoded as a checkpoint distinction without duplicating the reactive graph.
