// Per-entrant shim: the orchestrator rewrites this import at scoring time
// (see README.md). Checked-in target: the alt-b calibration adapter.
import adapter from '../calibration/alt-b-adapter';
export default adapter;
