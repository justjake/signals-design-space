# Signals Royale — status report (2026-07-08 evening)

Everything below is committed and durable; the only live work is CI. Read with
`royale/CANDIDATES.md` open for the slug lookup.

## Where things stand

**Judgement: complete. All 12 entries verified clean** through judge → fix →
re-judge cycles (scorecards: `royale/judgement/<slug>.md`, claimed-vs-observed with
battery tamper checks and independent probes). Two entries carry permanent notes:
sm1 an honesty warning (claimed a battery typecheck pass its own log showed
failing), sm2 an originality ruling honored (adopted the incumbent fork wholesale,
then rewrote it as its own 186-line protocol on order — rewrite verified byte-level
original).

**Ballots: complete** (`royale/ballots/`, synthesis in `royale/BALLOT-SYNTHESIS.md`).

**Perf: CI is the authoritative run** — workflow `royale-benchmark`, run
28980248690 on branch `royale/bench-ci` (12 entries + alien + 3 incumbents,
3 interleaved isolated rounds; sx1's hanging cellx suite excluded under a guard).
Fetch when done: `gh run download 28980248690 -n royale-benchmark`. A quick local
preview (single process, kairo suite only, 1 round — directional, not final) is
below.

## The size objectives (judge-verified, non-test LOC)

| Rank (fork) | Entry | Fork   | Lib      | vs incumbent fork 1510 / lib ~4800 |
| ----------- | ----- | ------ | -------- | ---------------------------------- |
| 1           | fx2   | **11** | 2239     | 137× / 2.1× smaller                |
| 2           | fm2   | 48     | 1734     | 31× / 2.8×                         |
| 3           | fx1   | 80     | 2343     | 19× / 2.0×                         |
| 4           | sh1   | 94     | **1217** | 16× / 3.9×                         |
| 5           | sx2   | 112    | 1367     | 13× / 3.5×                         |
| 6           | fh1   | 167    | 1957     | 9× / 2.5×                          |
| 7           | sm2   | 186    | 1524     | 8× / 3.1×                          |
| 8           | fm1   | 188    | 1780     | 8× / 2.7×                          |
| 9           | fh2   | 211    | 2740     | 7× / 1.8×                          |
| 10          | sh2   | 235    | 1424     | 6× / 3.4×                          |
| 11          | sm1   | 320    | 2146     | 5× / 2.2×                          |
| 12          | sx1   | 476    | 1295     | 3× / 3.7×                          |

## Perf preview (kairo suite, single process, 1 round — treat as directional)

Ratio vs Alien Signals: **fh2 1.24× — beats both incumbents** (Alt A 1.28×,
Alt B 1.28×; original Cosignal 0.99×). Then fx1 1.40×, fx2 1.45×, fm1 1.82×,
fh1 1.96×, sh2 1.97×, fm2 1.99×, sh1 2.85×, sm2 2.88×, sx1 2.98×, sx2 3.66×,
sm1 4.61×. CI's 3-round interleaved isolated run supersedes these numbers.

## Peer ballot verdict (advisory, not the ranking)

Peer mean ranking: **fh2 8.90 > fx2 8.75 > fh1 8.74 > fx1 8.65 > fm2 8.36 >
fm1 8.06 > sh1 7.15 > sm2 6.88 > sx2 6.65 > sh2 6.44 > sm1 6.11 > sx1 5.49.**
Notable: both model cohorts independently ranked all six fable entries above all
six sol entries (sol judges score ~+1.1 hotter in level but produce the same
ordering). Unanimous peer complaints: fh2's size/mount tax, fx2's residual
no-seam latest() corner. Full analysis with quotes: `royale/BALLOT-SYNTHESIS.md`.

## Early cross-entry findings (for the final report)

1. **The mutation window is the only irreducible fork surface.** fx2's 11-line
   fork with a per-line impossibility ledger proved the CONSTRAINTS.md ruling
   empirically: everything else the incumbent 1510-line fork does was achieved in
   userland over stock React APIs.
2. **The whole field independently converged on lane-facts seams** — React reports
   what it already knows (write lane, pass edges, mutation window, one lane-pin
   control); world semantics live in userland. Nobody rebuilt the in-reconciler
   batch registry.
3. **The `latest()` context rule was the hardest requirement**: 5 of 12 entries
   failed some shape of it (ambient-newest leaking into render bodies or computed
   evaluations) and it isn't covered by the shared battery — battery-v2 candidate.
4. **The judge → fix → re-judge loop found real bugs every time it ran**, including
   one introduced by a fix (fm1's suppression flag) and one only visible under heap
   pressure (fx2's finalizer arming). Every fix shipped regressions verified
   failing pre-fix.
5. **Perf is the field's honest weakness**: nobody matched the incumbents' alien
   parity; fh2 (attributed alien-v3 graph port + draft overlays) comes closest and
   the preview puts it ahead of alt-a/alt-b.

## What happens next (when the laptop reopens)

1. Fetch CI artifacts → authoritative core perf table (within-run ratios vs alien;
   CI is a 2-core runner, absolutes are not comparable to local).
2. Optional local react-bench re-runs (entrants' self-reported react tables are in
   their REPORT.md files meanwhile; all vs their own uSES baseline).
3. Assemble the final leaderboard (fork LOC → lib LOC → perf, all correctness-gated)
   and the tournament report at `royale/REPORT.md`.
